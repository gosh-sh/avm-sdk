[workspace]

[package]
name = "avm-standard-contract-b"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
avm-contract-sdk = { path = "../../crates/avm-contract-sdk" }
